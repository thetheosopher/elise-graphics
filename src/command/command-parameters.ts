/**
 * Typed command parameters for element command handlers.
 * Commands use different parameter types (TimerParameters, PointEventParameters, etc.)
 * so this is typed as unknown rather than a specific shape.
 */
export type CommandParameters = unknown;
